import { ProjectCard } from "./project-card/Cards";

export const ProjectsList = () => {
    return (
        <section className="container py-32">
            <ProjectCard />
        </section>
    );
};
